export interface AddressInfo {
  id: string | number;
  name: string;
  address: string;
  email: string;
  phone: string;
  notes: string;
}

export interface ItemInfo {
  id: number;
  value: string;
}
